{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Abstract Wiki Architect – Event: cultural event frame",
  "description": "Canonical semantic frame for cultural events (festivals, exhibitions, premieres, ceremonies, etc.) used by Abstract Wiki Architect.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "frame_type": {
      "type": "string",
      "description": "Canonical frame type name. Normalization code accepts several aliases, but JSON authored against this schema should prefer 'event.cultural'.",
      "enum": [
        "event.cultural",
        "event_cultural",
        "cultural_event"
      ]
    },

    "main_event": {
      "description": "The primary cultural event this frame describes (festival, exhibition, ceremony, premiere, etc.). This is typically the article subject.",
      "$ref": "#/definitions/Entity"
    },

    "event_kind": {
      "type": [
        "string",
        "null"
      ],
      "description": "High-level kind of cultural event, e.g. 'festival', 'film_festival', 'music_festival', 'exhibition', 'ceremony', 'premiere', 'awards_ceremony'."
    },

    "location": {
      "description": "Primary location (city, venue, region) where the cultural event takes place.",
      "anyOf": [
        {
          "$ref": "#/definitions/Entity"
        },
        {
          "type": "null"
        }
      ]
    },

    "time_span": {
      "description": "Approximate temporal span of the event (a specific date or a multi-day / multi-week period).",
      "anyOf": [
        {
          "$ref": "#/definitions/TimeSpan"
        },
        {
          "type": "null"
        }
      ]
    },

    "organizers": {
      "type": "array",
      "description": "Organizations or bodies responsible for organizing or hosting the event.",
      "items": {
        "$ref": "#/definitions/Entity"
      }
    },

    "participants": {
      "type": "array",
      "description": "Key participants associated with the event (artists, groups, delegations, juries). This list is intended to capture salient participants rather than an exhaustive roster.",
      "items": {
        "$ref": "#/definitions/Entity"
      }
    },

    "edition_number": {
      "type": [
        "integer",
        "null"
      ],
      "description": "Ordinal number of this edition, if the event is recurring (e.g. 75 for the '75th Venice International Film Festival')."
    },

    "recurring": {
      "type": [
        "boolean",
        "null"
      ],
      "description": "Whether the cultural event is part of a recurring series (annual festival, biennale, etc.)."
    },

    "series_entity": {
      "description": "If the event is one edition within a larger series, this is the entity representing the overall series (e.g. 'Cannes Film Festival').",
      "anyOf": [
        {
          "$ref": "#/definitions/Entity"
        },
        {
          "type": "null"
        }
      ]
    },

    "headline_audience_size": {
      "type": [
        "integer",
        "null"
      ],
      "description": "Approximate audience size or attendance count for the event (e.g. total spectators, visitors)."
    },

    "headline_audience_year": {
      "type": [
        "integer",
        "null"
      ],
      "description": "Reference year for the headline audience size, if it differs from the event year (e.g. multi-year averages)."
    },

    "domain_labels": {
      "type": "array",
      "description": "Lexical labels for the cultural domain(s) involved, e.g. 'film', 'music', 'theatre', 'art', 'literature'.",
      "items": {
        "type": "string"
      }
    },

    "attributes": {
      "type": "object",
      "description": "Free-form attribute bag for extensibility (e.g. award_categories, competition_sections, ticketing_info). Keys should be snake_case strings.",
      "additionalProperties": true
    }
  },

  "required": [
    "frame_type",
    "main_event"
  ],

  "definitions": {
    "Entity": {
      "description": "Lightweight entity reference used inside frames. It mirrors semantics.common.entity_base.Entity.",
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "id": {
          "type": [
            "string",
            "null"
          ],
          "description": "Stable identifier, typically a Wikidata QID such as 'Q42', or another project-specific ID."
        },
        "name": {
          "type": "string",
          "description": "Canonical surface label for the entity in the working language."
        },
        "gender": {
          "type": [
            "string",
            "null"
          ],
          "description": "Optional gender code for human entities, e.g. 'male', 'female', 'unknown'."
        },
        "human": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "Whether this entity is a human individual."
        },
        "entity_type": {
          "type": [
            "string",
            "null"
          ],
          "description": "Optional coarse type hint such as 'organization', 'person', 'place', 'event'."
        },
        "lemmas": {
          "type": "array",
          "description": "Optional list of lemma strings associated with the entity, e.g. profession, role, or domain lemmas.",
          "items": {
            "type": "string"
          }
        },
        "features": {
          "type": "object",
          "description": "Arbitrary feature dictionary (e.g. grammatical, semantic, or domain-specific flags).",
          "additionalProperties": true
        },
        "extra": {
          "type": "object",
          "description": "Arbitrary metadata attached to the entity.",
          "additionalProperties": true
        }
      },
      "required": [
        "name"
      ]
    },

    "TimeSpan": {
      "description": "Coarse time span representation as used in semantics.common.entity_base.TimeSpan.",
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "start_year": {
          "type": [
            "integer",
            "null"
          ],
          "description": "Starting year, e.g. 1998."
        },
        "end_year": {
          "type": [
            "integer",
            "null"
          ],
          "description": "Optional end year, e.g. 2005."
        },
        "start_month": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 1,
          "maximum": 12,
          "description": "Optional starting month (1-12)."
        },
        "start_day": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 1,
          "maximum": 31,
          "description": "Optional starting day of month (1-31)."
        },
        "end_month": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 1,
          "maximum": 12,
          "description": "Optional end month (1-12)."
        },
        "end_day": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 1,
          "maximum": 31,
          "description": "Optional end day of month (1-31)."
        },
        "approximate": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "Whether the dates are approximate or inferred."
        },
        "extra": {
          "type": "object",
          "description": "Arbitrary metadata related to the time span.",
          "additionalProperties": true
        }
      }
    }
  }
}
