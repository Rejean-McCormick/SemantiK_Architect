# docker-compose.yml
version: '3.8'

services:
  # 1. MESSAGE BROKER
  # The central nervous system for async tasks (Arq/Celery)
  redis:
    image: redis:alpine
    container_name: aw_redis
    ports:
      - "6379:6379"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # 2. API BACKEND (Hexagonal Adapter)
  # Serves HTTP requests, publishes events to Redis
  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    container_name: aw_backend
    env_file: .env
    environment:
      - REDIS_HOST=redis
      # CRITICAL: Force the container path to match the volume mount
      # This overrides any Windows paths in your local .env
      - FILESYSTEM_REPO_PATH=/app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
      redis:
        condition: service_healthy
    # Use factory flag because 'create_app' is a function
    command: uvicorn app.adapters.api.main:create_app --host 0.0.0.0 --port 8000 --reload --factory
    restart: unless-stopped

  # 3. ASYNC WORKER (Hexagonal Adapter)
  # Consumes 'BuildLanguage' and 'Audit' events from Redis
  worker:
    build:
      context: .
      dockerfile: docker/Dockerfile.worker
    container_name: aw_worker
    env_file: .env
    environment:
      - REDIS_HOST=redis
      # CRITICAL: Must match Backend's path so they see the same PGF file
      - FILESYSTEM_REPO_PATH=/app
    volumes:
      - .:/app
    depends_on:
      redis:
        condition: service_healthy
    # [FIX] Use the standard 'arq' CLI to run the settings class we just updated
    # --watch app enables code hot-reloading during dev
    command: arq app.workers.worker.WorkerSettings --watch app
    restart: unless-stopped

  # 4. FRONTEND (UI Layer)
  # Next.js application consuming the Backend API
  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
    container_name: aw_frontend
    env_file: .env
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - backend
    ports:
      - "3000:3000"
    restart: unless-stopped

  # 5. REVERSE PROXY (Optional/Prod)
  # Routes traffic between Frontend and Backend
  nginx:
    image: nginx:alpine
    container_name: aw_nginx
    depends_on:
      - frontend
      - backend
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "4000:80"
    restart: unless-stopped